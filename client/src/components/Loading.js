import React from "react";
import { Icon } from "antd";

const Loading = () => (
  <div
    style={{
      fontSize: "3em",
      textAlign: "center",
      lineHeight: "60vh"
    }}
  >
    <Icon type="loading" />
  </div>
);

export default Loading;
