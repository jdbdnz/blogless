import React from "react";
import { Icon } from "antd";

const Loading = () => (
  <div
    style={{
      fontSize: "3em",
      textAlign: "center",
      lineHeight: "100vh"
    }}
  >
    <Icon type="loading" />
  </div>
);

export default Loading;
